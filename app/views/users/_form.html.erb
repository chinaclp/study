<%= form_for(@user) do |f| %> 
  <% if @user.errors.any? %>  
    <div id="errorExplanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>  
      <ul>  
      <% @user.errors.full_messages.each do |msg| %>  
         <li><%= msg %></li>  
      <% end %>  
      </ul>  
    </div>  
  <% end %>

  <div class="field">  
       <%= f.label :name %><br />  
       <%= f.text_field :name %>  
  </div>  
  <div class="field">  
        <%=  select("klass", "klass_id", Klass.all.collect {|p| [ p.name, p.id ] },  {:prompt => '--请选择班级--'})%>  
   #    <%= f.label :klass %><br />  
   #    <%= f.text_field :klass %>  
  </div> 

  <div class="actions">  
       <%= f.submit "提交"%>  
  </div>  
<% end %>
