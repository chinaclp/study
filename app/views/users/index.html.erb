<h1>hello!</h1>
 
<table>
  <tr>
    <th>name</th> 
    <th>klass</th> 
  </tr> 
  
  <% @users.each do |user| %>
     <tr>
        <td><%= user.name %></td>
        <td><%= user.klass %></td> 
     </tr>
  <% end %>
</table>

<%= link_to 'New user', new_user_path %>
