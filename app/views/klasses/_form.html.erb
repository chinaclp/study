<%= form_for(@klass) do |f| %>
  <% if @klass.errors.any? %>
    <div id="errorExplanation">
      <h2><%= pluralize(@klass.errors.count, "error") %> prohibited this klass from being     saved:</h2>
      <ul>
        <% @klass.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class= "field">
    <%= f.label "填写班级名称"%><br/>
    <%= f.text_field :klass_name%>
  </div>

  <div class= "actions">
    <%= f.submit "提交"%>
  </div>
<%end%>  
