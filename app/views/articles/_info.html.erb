  <li id="comment_<%= comment.id%>">
  <%= comment.content %>
  <%= link_to "删除", article_comment_path(article_id: @article.id, id: comment.id, format: "json"), :method => 'delete', class: "del_button", id: "del_#{comment.id}", remote: true%>
  </li> 
